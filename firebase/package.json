{
  "scripts": {
    "start": "concurrently \"npm run test:firestore\"",
    "test:firestore": "npm run test --prefix=firestore",
    "test:firestoreCI": "npm run testCI --prefix=firestore"
  },
  "dependencies": {
    "concurrently": ">=6.5.1"
  },
  "devDependencies": {
    "firebase-tools": ">=10.2.0"
  },
  "engines": {
    "node": ">=14"
  }
}